#!/bin/sh

mkdir -p ~/.ssh/

ln -sf $XDG_CONFIG_HOME/ssh/config ~/.ssh/
# TODO: in the future, delete the old control-sockets directory
#rm -rf $XDG_CACHE_HOME/ssh/control-sockets/
mkdir -p $XDG_CACHE_HOME/ssh/cs/
